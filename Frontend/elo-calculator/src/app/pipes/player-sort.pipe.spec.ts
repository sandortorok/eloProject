import { PlayerSortPipe } from './player-sort.pipe';

describe('PlayerSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerSortPipe();
    expect(pipe).toBeTruthy();
  });
});
